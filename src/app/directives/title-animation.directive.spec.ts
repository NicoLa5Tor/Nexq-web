import { TitleAnimationDirective } from './title-animation.directive';

describe('TitleAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
